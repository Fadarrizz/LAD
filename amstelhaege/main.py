
from algorithms.randomfunction import *
from classes import *
from helpers import *

amount = Amount()

Grid(random(amount), Variant(Amount), amount, TotalScore.totalScore())

# # Show map
plt.show()
